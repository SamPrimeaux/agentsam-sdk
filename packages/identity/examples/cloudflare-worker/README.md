# Cloudflare Worker + D1 example (Phase 7)

Minimal worker wiring for `@inneranimalmedia/agentsam-sdk` identity with `adapters/cloudflare-d1`.

Scaffold lands after IAM adapter proves the storage contract. Alpha today: preview portal + provider contracts.

```bash
npm install @inneranimalmedia/agentsam-sdk@alpha
npx agentsam identity preview --open
```
