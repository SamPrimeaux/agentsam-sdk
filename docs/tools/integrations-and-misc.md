# Integrations & Misc

Google Drive, Resend (inactive), and raw HTTP fetch (inactive).

**5 tools** in this domain.

## `integrations.execute` (4)

- **`agentsam_gdrive`** (Google Drive) — Reads files and folders from Google Drive via IAM gdrive proxy tools. _never used_
- **`gdrive_list`** (Google Drive List) — List files in Google Drive with optional query filter. _**inactive**, never used_
- **`resend_send_broadcast`** (Resend Broadcast) — Send a broadcast email to an audience via Resend. Requires approval. _**inactive**, risk: high, never used_
- **`resend_send_email`** (Resend Send Email) — Send a transactional email via Resend API. Requires from/to/subject/html. _**inactive**, never used_

## `network` (1)

- **`http_fetch`** (HTTP Fetch) — Fetch a URL via HTTP GET. Returns response body as text. Use for external APIs without auth. _**inactive**, never used_
