# AgentSam SDK — Identity module

See [`packages/identity/README.md`](../../packages/identity/README.md).

Customer import:

```js
import { createIdentityClient } from '@inneranimalmedia/agentsam-sdk';
```

Alpha: `@inneranimalmedia/agentsam-sdk@2.0.0-alpha.identity.1` (tag `alpha`)

Preview auth portal locally:

```bash
npx agentsam identity preview --open
```
