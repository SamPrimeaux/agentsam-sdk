# app/

- `frontend/` — Studio UI (TanStack). Until `scripts/restructure-app-layout.sh` runs, the live UI is still repo-root `src/`.
- `backend/` — Workers, wrangler, D1 migrations, LLM proxy stub.

Local model (Ollama) is not here. It stays on the iMac LaunchAgent.
See `docs/APP_LAYOUT.md` and `docs/local-ollama-workhorse.md`.
